import React from 'react';
import App from './app';

export default function IncrementButton(props) {
    return (
        <button 
            onClick={this.handleClicks} type='button'>
            Increment
        </button>
    )
}
